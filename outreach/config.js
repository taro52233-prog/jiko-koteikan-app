// ============================================================
// 設定ファイル
// ここを編集すれば挙動を変えられます（コードは触らなくてOK）
// ============================================================

export const config = {
  // ── ① リスト収集（Google Places API）─────────────────
  search: {
    // 検索キーワード（複数入れると網羅性が上がる。place_id で自動重複排除）
    queries: [
      "堺市 葬儀社",
      "堺市 葬儀屋",
      "堺市 斎場",
      "堺市 セレモニーホール",
      "堺市 家族葬",
    ],
    // 「堺市近辺」の中心座標（堺市役所付近）と半径(m)。近隣市も拾いたい時は広げる
    center: { lat: 34.5733, lng: 135.4831 },
    radiusMeters: 15000, // 15km（堺市＋近隣まで）
    language: "ja",
    region: "jp",
  },

  // ── ② メールアドレス抽出 ─────────────────────────────
  extract: {
    requestTimeoutMs: 15000,
    // 問い合わせページを探して1階層だけ追加で見に行く
    followContactPage: true,
    // サイト巡回の間隔(ms)。相手サーバに優しく
    politeDelayMs: 1500,
  },

  // ── ③ 文面生成 ───────────────────────────────────────
  // {会社名} は差し込みされます。他の {キー} も leads の列名で差し込み可能
  template: {
    subject: "【遺品整理のご提案】{会社名} ご担当者さまへ",
    body: `{会社名}
ご担当者さま

突然のご連絡失礼いたします。
遺品整理サービスを行っております、{自社名}の{担当者名}と申します。

貴社にご葬儀をご依頼されたご遺族さまの中には、
その後の「遺品整理」でお困りになる方も少なくありません。

私どもは、ご遺族さまに寄り添った遺品整理・生前整理を承っており、
貴社と提携させていただくことで、ご遺族さまへ切れ目のないサポートを
ご提供できればと考えております。

・遺品整理／生前整理／特殊清掃
・見積り無料、堺市近辺を中心に即日対応可
・貴社経由のご紹介には紹介手数料もご相談可能です

一度お電話またはメールにてお打ち合わせのお時間を頂けますと幸いです。
ご検討のほど、よろしくお願い申し上げます。`,
  },

  // ── ④ 送信（Gmail）───────────────────────────────────
  send: {
    dailyLimit: 30, // 1日の送信上限（Gmailの制限＆迷惑判定回避のため控えめに）
    minIntervalMs: 30000, // 送信間隔の最小(ms)。30秒
    maxIntervalMs: 90000, // 送信間隔の最大(ms)。ランダムで人間らしく
  },
};

// ============================================================
// 送信者情報（特定電子メール法で表示が必須）
// 環境変数(.env)から読み込み。未設定だと送信をブロックします
// ============================================================
export const sender = {
  companyName: process.env.SENDER_COMPANY || "",   // 自社名（会社名）
  personName: process.env.SENDER_PERSON || "",     // 担当者名
  address: process.env.SENDER_ADDRESS || "",       // 住所（必須）
  tel: process.env.SENDER_TEL || "",               // 電話
  email: process.env.GMAIL_USER || "",             // 連絡先メール（＝送信元）
  website: process.env.SENDER_WEBSITE || "",       // 任意
};
